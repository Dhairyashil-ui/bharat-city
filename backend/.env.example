# Groq AI (optional — for smart suggestions)
GROQ_API_KEY=your_groq_api_key_here

# MySQL database (optional — app runs without it)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=smart_energy

# Server port (set automatically by Render)
PORT=5000
